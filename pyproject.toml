[project]
name = "pyrefactor"
version = "0.1.0"
description = "AST-based refactoring tool to lift safe imports to top-level"
requires-python = ">=3.9"
authors = [{ name = "Auto-generated", email = "devnull@example.com" }]
dependencies = [
    "libcst>=1.1.0",
]

[project.scripts]
pyrefactor = "pyrefactor.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
